\usepackage{tikz}
\usepackage{xparse}
% =========================================================
% Required TikZ Libraries
% =========================================================
\usetikzlibrary{
    positioning,
    fit,
    calc,
    arrows.meta,
    arrows,
    decorations.pathreplacing,
    shapes.geometric,
    shapes.symbols,
    shadows
}

% Fallback for Stealth arrows if arrows.meta has issues
\tikzset{>=stealth}

% =========================================================
% Semantic Colors
% =========================================================
\definecolor{irSparse}{HTML}{3498DB}
\definecolor{irDense}{HTML}{E67E22}
\definecolor{irRerank}{HTML}{C0392B}
\definecolor{irHybrid}{HTML}{2ECC71}
\definecolor{irANN}{HTML}{9B59B6}
\definecolor{irInfra}{HTML}{58595B}
\definecolor{irPrimary}{HTML}{3498DB}
\definecolor{irAccent}{HTML}{E67E22}
\definecolor{irSecondary}{HTML}{C0392B}

% Accent can be changed globally if needed
\colorlet{irAccent}{irDense}

% =========================================================
% Base TikZ Styles
% =========================================================
\tikzset{
    >=Latex,
    font=\small,
    node distance=1.7cm and 2.2cm
}

% =========================================================
% Base Box Style
% =========================================================
\tikzset{
    irBoxBase/.style={
        draw,
        rounded corners=2pt,
        thick,
        minimum height=1.2cm,
        text width=3.5cm,
        align=center,
        inner sep=5pt,
        outer sep=2pt,
        font=\small
    },
    % Use this to override standard lecture-box dimensions for small components
    compact/.style={
        text width={},
        minimum height=0.5cm,
        minimum width=0.5cm,
        inner sep=2pt,
        font=\tiny
    }
}

% =========================================================
% Semantic Variants
% =========================================================
\tikzset{
    sparse/.style={
        irBoxBase,
        draw=irSparse,
        fill=irSparse!10,
        text=black
    },
    dense/.style={
        irBoxBase,
        draw=irDense,
        fill=irDense!12,
        text=black
    },
    rerank/.style={
        irBoxBase,
        draw=irRerank,
        fill=irRerank!10,
        text=black
    },
    hybrid/.style={
        irBoxBase,
        draw=irHybrid!70!black,
        fill=irHybrid!12,
        text=black
    },
    ann/.style={
        irBoxBase,
        draw=irANN,
        fill=irANN!12,
        text=black
    },
    infra/.style={
        irBoxBase,
        draw=irInfra,
        fill=irInfra!10,
        text=black
    },
    muted/.style={
        irBoxBase,
        draw=irInfra!45,
        fill=irInfra!6,
        text=irInfra
    },
    ghost/.style={
        irBoxBase,
        draw=irInfra!20,
        fill=white,
        text=irInfra!80
    },
    emph/.style={
        irBoxBase,
        draw=black,
        fill=yellow!15,
        line width=1.1pt,
        text=black
    }
}

% =========================================================
% Labels
% =========================================================
\tikzset{
    irLabel/.style={
        font=\scriptsize,
        text=irInfra,
        align=center
    },
    irArrowLabelAbove/.style={
        above,
        pos=0.5,
        font=\scriptsize,
        text=irInfra,
        fill=white,
        inner sep=1pt,
        align=center
    },
    irArrowLabelRight/.style={
        right,
        pos=0.5,
        font=\scriptsize,
        text=irInfra,
        fill=white,
        inner sep=1pt,
        align=center
    },
    irArrowLabelBox/.style={
        pos=0.55,
        font=\scriptsize,
        text=irInfra,
        fill=white,
        inner sep=1pt,
        align=center
    }
}

% =========================================================
% Arrow Styles
% =========================================================
\tikzset{
    irArrowBase/.style={
        -{Stealth[length=3mm]},
        draw=irInfra,
        line width=0.8pt,
        shorten >=2pt,
        shorten <=2pt
    },
    irArrowMuted/.style={
        -{Latex[length=2mm,width=1.4mm]},
        draw=irInfra!50,
        line width=0.7pt,
        shorten >=2pt,
        shorten <=2pt
    }
}

% =========================================================
% Group Containers
% =========================================================
\tikzset{
    irGroupBase/.style={
        draw,
        dashed,
        rounded corners,
        inner sep=0.45cm
    }
}

% =========================================================
% Document Stack
% =========================================================
\tikzset{
    irDocStack/.style={
        irBoxBase,
        draw=irInfra,
        fill=white,
        text width=2.5cm,
        minimum height=0.85cm,
        double copy shadow={shadow xshift=2pt, shadow yshift=-2pt}
    }
}

% =========================================================
% Database Shape
% =========================================================
\tikzset{
    irDatabase/.style={
        cylinder,
        shape border rotate=90,
        aspect=0.3,
        draw=irInfra,
        fill=irInfra!8,
        minimum height=1.1cm,
        minimum width=1.6cm,
        align=center,
        font=\small
    }
}

% =========================================================
% Telescope / Funnel Tier
% =========================================================
\tikzset{
    irTelescopeBase/.style={
        trapezium,
        trapezium stretches body,
        trapezium left angle=120,
        trapezium right angle=120,
        draw,
        minimum height=0.7cm,
        inner sep=2pt,
        align=center,
        font=\small
    }
}

% =========================================================
% Legacy & LTR Compatibility
% =========================================================
% These are kept to avoid breaking existing lecture slides (e.g. 06_ltr.tex)
\definecolor{irPointwise}{HTML}{3498DB}
\definecolor{irPairwise}{HTML}{C0392B}
\definecolor{irListwise}{HTML}{1ABC9C}

\tikzset{
    pointwise/.style={sparse},
    pairwise/.style={rerank},
    listwise/.style={hybrid, draw=irListwise, fill=irListwise!10},
    primary/.style={sparse},
    accent/.style={dense},
    secondary/.style={rerank},
    info/.style={font=\scriptsize\itshape, text=irInfra!80},
    % Helper for legacy arrows
    arr/.style={irArrowBase}
}

% Additional missing colors from legacy
\definecolor{irMuted}{HTML}{D9D9D9}
